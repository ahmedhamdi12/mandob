import 'package:flutter/material.dart';

class AppColors {
  // Primary (Teal Green)
  static const Color primary = Color(0xFF0D5C63);
  static const Color primaryDark = Color(0xFF094147);
  static const Color onPrimary = Color(0xFFFFFFFF);

  // Secondary (Vibrant Orange)
  static const Color secondary = Color(0xFFE8611A);
  static const Color secondaryLight = Color(0xFFF28C28);
  static const Color onSecondary = Color(0xFFFFFFFF);

  // Accent (Fresh Green)
  static const Color accent = Color(0xFF4CAF50);

  // Background & Surface
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Color(0xFFFFFFFF);

  // Status Colors
  static const Color error = Color(0xFFD32F2F);
  static const Color warning = Color(0xFFFFA000);
  static const Color success = Color(0xFF388E3C);

  // Text Colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  
  // Dividers & Borders
  static const Color border = Color(0xFFE0E0E0);
  static const Color divider = Color(0xFFEEEEEE);
}
